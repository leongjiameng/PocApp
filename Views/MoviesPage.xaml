<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PocApp.MoviesPage"
             Title="Movies">

    <Grid Padding="16"
            RowDefinitions="Auto,*,Auto">

        <Label
            x:Name="StatusLabel"
            Grid.Row="0"
            Text=""
            HorizontalOptions="Center"
            Margin="0,0,0,8"/>

        <!-- Pull-to-refresh + infinite scroll -->
        <RefreshView
            x:Name="MoviesRefreshView"
            Grid.Row="1"
            Refreshing="OnRefreshRequested">

            <CollectionView
                x:Name="MoviesCollectionView"
                RemainingItemsThreshold="3"
                RemainingItemsThresholdReached="OnRemainingItemsThresholdReached">

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Margin="0,4"
                               Padding="10"
                               HasShadow="False">
                            <VerticalStackLayout Spacing="4">
                                <Label Text="{Binding Title}"
                                       FontAttributes="Bold"
                                       FontSize="16"/>

                                <Label>
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="{Binding CountryOfOrigin}"/>
                                            <Span Text=" • "/>
                                            <Span Text="{Binding DateOfRelease, StringFormat='Release: {0:yyyy-MM-dd}'}"/>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>

                                <Label Text="{Binding Description}"
                                       FontSize="13"
                                       LineBreakMode="WordWrap"/>
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </RefreshView>

        <ActivityIndicator
            x:Name="LoadingIndicator"
            Grid.Row="2"
            IsVisible="False"
            IsRunning="False"
            HorizontalOptions="Center"
            Margin="0,8,0,0"/>
    </Grid>
</ContentPage>